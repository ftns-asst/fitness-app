; Настройки qmllint проекта (QML линтуется по docs/CODE_STYLE.md §0: конвенции Qt QML).
; UnqualifiedAccess: делегаты демо используют стандартные контекстные свойства модели
; (modelData, index), а каркасные id читаются из контекста создания компонента.
; Перевод делегатов на `pragma ComponentBehavior: Bound` + required properties
; запланирован на продуктовом этапе (M1+), поэтому категория отключена временно.
[Warnings]
UnqualifiedAccess=disable
